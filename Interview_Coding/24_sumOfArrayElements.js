let arr = [20,33,8,3,2,8];
let sum = 0;
for(let i=0; i<arr.length; i++){
    sum +=arr[i];
    //sum = sum +arr[i]
}
console.log(sum);

//I initialized a variable sum to 0, traversed the array once, added each element to sum, and printed the final result.